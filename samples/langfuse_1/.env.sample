# Copy this file to `.env` and add your Anthropic key.
#   cp .env.sample .env
#
# docker-compose bootstraps Langfuse and its API keys for you; the only secret
# you provide is the model key for the call being traced.
ANTHROPIC_API_KEY=
