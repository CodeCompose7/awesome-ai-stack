# Copy to `.env` and add your Anthropic API key (or swap MODEL for another
# provider that LiteLLM supports, with the matching key).
#   cp .env.sample .env
ANTHROPIC_API_KEY=

MODEL=anthropic/claude-opus-4-8
