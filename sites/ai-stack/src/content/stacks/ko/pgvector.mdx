---
name: pgvector
vendor: "pgvector"
category: vec-db
docVersion: "1.0"
updated: 2026-06-28
description: 벡터 유사도 검색을 위한 Postgres 확장 — 별도 DB 없이 에이전트 메모리와 RAG를 추가합니다.
website: https://github.com/pgvector/pgvector
repo: https://github.com/pgvector/pgvector
tags: [embeddings, rag, postgres, similarity-search]
language: SQL / C
license: PostgreSQL
pricing: [open-source]
pricingNote: "무료 PostgreSQL 확장(PostgreSQL 라이선스)이며, 사용하는 Postgres 비용만 부담합니다."
pricingSource: https://github.com/pgvector/pgvector
pricingCheckedAt: "2026-06-27"
projects:
  - folder: pgvector_1
    related: [langgraph, litellm]
samples:
  - version: "Schema · vector column"
    lang: sql
    diagram: |
      flowchart LR
        text[text] -->|embedding model| vec[vector]
        vec --> pg[(Postgres + pgvector)]
        q[query vector] -->|<=> cosine| pg
        pg --> top[top-k rows]
    code: |
      CREATE EXTENSION IF NOT EXISTS vector;

      CREATE TABLE memories (
        id        bigserial PRIMARY KEY,
        body      text,
        embedding vector(1536)        -- match your embedding model's dimensions
      );

      -- Nearest-neighbour search using cosine distance (<=>):
      SELECT body
      FROM memories
      ORDER BY embedding <=> $1
      LIMIT 5;
    description: |
      ### 언제 쓰나
      관계형 데이터 옆에 임베딩을 저장하고 SQL로 유사도 검색을 하고 싶을 때
      쓰세요.

      ### 흐름
      임베딩 모델이 텍스트를 벡터로 바꾸고, `<=>`(코사인 거리) 연산자로 최근접
      이웃을 정렬해 조회합니다.
    note: 실제로 돌려보려면 구현 샘플 탭의 pgvector_1을 보세요 — Postgres와 앱을 docker compose로 띄웁니다.
  - version: "Index · HNSW"
    lang: sql
    diagram: |
      flowchart LR
        rows[(many rows)] --> idx[HNSW index]
        q[query] --> idx
        idx --> approx[approx top-k]
    code: |
      -- For large tables, add an approximate index to keep queries fast.
      CREATE INDEX ON memories USING hnsw (embedding vector_cosine_ops);

      -- Same query as before — now answered with the index.
      SELECT body FROM memories ORDER BY embedding <=> $1 LIMIT 5;
    description: |
      ### 언제 쓰나
      테이블이 커져 정확 검색(full scan)이 느려질 때 근사 인덱스를 추가하세요.

      ### 흐름
      `hnsw` 인덱스가 근사 최근접 이웃 그래프를 만들어, 약간의 정확도를 내주고
      쿼리를 크게 빠르게 합니다.
---

## 개요

`pgvector`는 PostgreSQL에 `vector` 컬럼 타입과 유사도 연산자를 추가합니다.  
이미 Postgres를 운영 중이라면, 에이전트에 장기 기억이나 RAG 검색을 붙이는 가장 마찰 없는 방법입니다 — 추가로 운영할 서비스가 없습니다.

```mermaid
flowchart LR
  text[text] -->|embedding model| vec[vector]
  vec --> pg[(Postgres + pgvector)]
  q[query vector] -->|<=> cosine| pg
  pg --> top[top-k rows]
```

**코드 샘플** 탭에는 스키마·조회와 인덱스 예시가 있습니다 — 선택기에서 비교해 보세요.

## 언제 쓰면 좋은가

벡터 검색을 관계형 데이터와 같은 곳에 두고 싶고, 전용 벡터 스토어를 추가하기 보다 하나의 데이터베이스만 운영하고 싶을 때 pgvector를 선택하세요.
