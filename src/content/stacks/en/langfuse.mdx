---
name: Langfuse
vendor: "Langfuse"
logo: /logos/langfuse.svg
category: observability-tracing
docVersion: "1.0"
updated: 2026-06-28
description: Open-source tracing, evaluation, and analytics for LLM and agent applications
website: https://langfuse.com
repo: https://github.com/langfuse/langfuse
docs: https://langfuse.com/docs
tags: [tracing, eval, monitoring, analytics]
language: TypeScript
license: MIT
pricing: [open-source, free-tier, paid]
pricingNote: "Open source, free to self-host. Langfuse Cloud is the managed observability service."
pricingTiers:
  - { plan: "Hobby", price: "$0", note: "50k units/mo" }
  - { plan: "Core", price: "$29/mo", note: "Higher limits" }
  - { plan: "Pro", price: "$199/mo", note: "Team features" }
  - { plan: "Enterprise", price: "$2,499/mo", note: "SSO, SLA, support" }
pricingSource: https://langfuse.com/pricing
pricingCheckedAt: "2026-06-27"
projects:
  - folder: langfuse_1
    related: [anthropic-claude]
samples:
  - version: "TypeScript · trace"
    lang: ts
    diagram: |
      flowchart LR
        run[agent run] --> trace[trace]
        trace --> gen[generation span]
        trace --> tool[tool span]
        trace --> ui[(Langfuse UI)]
    code: |
      import { Langfuse } from 'langfuse';

      const langfuse = new Langfuse(); // reads LANGFUSE_* env vars

      const trace = langfuse.trace({ name: 'support-agent', userId: 'u_123' });

      const gen = trace.generation({
        name: 'plan',
        model: 'claude-opus-4-8',
        input: 'How do I reset my password?',
      });

      // ... call the model ...
      gen.end({ output: 'Tell the user to use the reset link.' });

      await langfuse.flushAsync();
    description: |
      ### When to use
      Reach for the SDK when you want fine-grained tracing, creating each step as
      an explicit span.

      ### How it flows
      Nest model and tool spans under a trace, then flush to send them to
      Langfuse.
  - version: "Python · @observe"
    lang: python
    diagram: |
      flowchart LR
        fn["@observe function"] --> span[auto span]
        span --> ui[(Langfuse UI)]
    code: |
      from langfuse import observe, get_client

      @observe()  # wraps the call in a trace/span automatically
      def plan(question: str) -> str:
          # ... call the model ...
          return "Tell the user to use the reset link."

      plan("How do I reset my password?")
      get_client().flush()  # send buffered events before exit
    description: |
      ### When to use
      Reach for the decorator when you want easy, function-level
      auto-instrumentation.

      ### How it flows
      `@observe` wraps the function call in a span automatically and sends its
      inputs/outputs to Langfuse.
    note: For a runnable, self-hosted trace round-trip, see langfuse_1 in the Implementation tab.
---

## Overview

Agents fail in ways that are hard to see from logs alone.  
Langfuse records each run as a **trace** of nested spans (model calls, tool calls, retrievals) so you can inspect latency, cost, and the exact prompts/outputs at every step — then attach scores and evals on top.

```mermaid
flowchart LR
  run[agent run] --> trace[trace]
  trace --> gen[generation span]
  trace --> tool[tool span]
  trace --> ui[(Langfuse UI)]
```

The **Code samples** tab shows the TypeScript SDK and the Python decorator — pick from the selector to compare.

## When to use it

Add Langfuse early when you need to debug multi-step agent runs, compare prompt versions, or track cost and quality in production.
