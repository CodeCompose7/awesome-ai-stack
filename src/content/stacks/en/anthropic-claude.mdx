---
name: Anthropic Claude
logo: /logos/anthropic.jpeg
category: llm-foundation
description: Frontier LLM API with strong tool-use and long-context reasoning, ideal as an agent's core model.
website: https://www.anthropic.com
docs: https://docs.anthropic.com
tags: [llm, tool-use, reasoning, api]
language: API
license: proprietary
pricing: [paid]
pricingNote: "Paid API billed per token; Batch is 50% off and prompt caching up to 90% off. Prices per million tokens (input / output)."
pricingTiers:
  - { plan: "Claude Opus 4.8", price: "$5 / $25", note: "Most capable." }
  - { plan: "Claude Sonnet 4.6", price: "$3 / $15", note: "Balanced." }
  - { plan: "Claude Haiku 4.5", price: "$1 / $5", note: "Fastest, cheapest." }
pricingSource: https://www.anthropic.com/pricing
pricingCheckedAt: "2026-06-27"
projects:
  - folder: anthropic-claude_1
    related: []
featured: true
samples:
  - version: "TypeScript · tool use"
    lang: ts
    diagram: |
      flowchart LR
        user[user message] --> claude[Claude]
        claude -->|tool_use| tool[run tool]
        tool -->|tool_result| claude
        claude --> answer[final answer]
    code: |
      import Anthropic from '@anthropic-ai/sdk';

      const client = new Anthropic(); // reads ANTHROPIC_API_KEY

      const tools = [
        {
          name: 'get_weather',
          description: 'Get the current weather for a city',
          input_schema: {
            type: 'object',
            properties: { city: { type: 'string' } },
            required: ['city'],
          },
        },
      ] as const;

      const message = await client.messages.create({
        model: 'claude-opus-4-8',
        max_tokens: 1024,
        tools,
        messages: [{ role: 'user', content: 'What should I wear in Seoul today?' }],
      });

      // When Claude wants a tool, it returns a `tool_use` block. Run the tool,
      // then send the result back as a `tool_result` to continue the loop.
      for (const block of message.content) {
        if (block.type === 'tool_use') {
          console.log('call', block.name, block.input);
        }
      }
    description: |
      ### When to use
      Reach for tool use when you want the model to call functions and take
      external actions.

      ### How it flows
      Claude returns a `tool_use` block; you run the tool and send the result
      back as a `tool_result` to continue the loop.
    note: For a runnable build of this loop, see anthropic-claude_1 in the Implementation tab.
  - version: "Python · messages"
    lang: python
    diagram: |
      flowchart LR
        msg[messages.create] --> claude[Claude]
        claude --> text[text reply]
    code: |
      from anthropic import Anthropic

      client = Anthropic()  # reads ANTHROPIC_API_KEY

      message = client.messages.create(
          model="claude-opus-4-8",
          max_tokens=1024,
          messages=[{"role": "user", "content": "Summarize Claude in one line."}],
      )
      print(message.content[0].text)
    description: |
      ### When to use
      Reach for this when you need a simple one-shot message call from Python.

      ### How it flows
      `messages.create` returns the model's reply; the text lives in
      `content[0].text`.
---

## Overview

Claude is Anthropic's family of frontier models.  
For agentic workloads it is a strong default thanks to reliable **tool use** (function calling), large context windows, and steerable instruction-following.

Common model ids (use the latest/most capable for new agents):

- `claude-opus-4-8` — most capable, best for hard reasoning and long agent loops
- `claude-sonnet-4-6` — balanced cost/latency for high-volume agents
- `claude-haiku-4-5-20251001` — fastest/cheapest for simple sub-tasks

```mermaid
flowchart LR
  user[user message] --> claude[Claude]
  claude -->|tool_use| tool[run tool]
  tool -->|tool_result| claude
  claude --> answer[final answer]
```

The **Code samples** tab shows TypeScript and Python examples — pick from the
selector to compare.

## When to use it

Reach for Claude when your agent needs dependable multi-step tool use, careful
instruction-following, or long-context document reasoning.
