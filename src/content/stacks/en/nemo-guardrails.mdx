---
name: NeMo Guardrails
vendor: "NVIDIA"
category: guardrails
description: Programmable rails for LLM apps — keep conversations safe, on-topic, and on-policy with Colang flows.
website: https://github.com/NVIDIA/NeMo-Guardrails
repo: https://github.com/NVIDIA/NeMo-Guardrails
docs: https://docs.nvidia.com/nemo/guardrails
tags: [guardrails, safety, dialogue, python]
language: Python
license: Apache-2.0
pricing: [open-source]
pricingNote: "Free and open source from NVIDIA; the enterprise NeMo microservices platform is a separate product."
pricingSource: https://github.com/NVIDIA/NeMo-Guardrails
pricingCheckedAt: "2026-06-27"
related: [guardrails-ai]
projects:
  - folder: nemo-guardrails_1
    related: [openai]
samples:
  - version: "Python · add rails"
    lang: python
    diagram: |
      flowchart LR
        user[user] --> rails[NeMo Guardrails]
        rails --> policy[Colang rails]
        rails --> llm[LLM]
        llm --> safe[on-topic reply]
    code: |
      from nemoguardrails import LLMRails, RailsConfig

      # Load config.yml + Colang flows from a directory.
      config = RailsConfig.from_path("./config")
      rails = LLMRails(config)

      response = rails.generate(messages=[
          {"role": "user", "content": "Tell me about your refund policy."},
      ])
      print(response["content"])
    description: |
      ### When to use
      Reach for NeMo Guardrails when an LLM app must stay on-topic and on-policy,
      with input, dialog, and output rails enforced before and after the model.

      ### How it flows
      `RailsConfig` loads your Colang rails; `generate` runs the user turn through
      them and the LLM, returning a checked, on-topic reply.
---

## Overview

NeMo Guardrails is an open-source toolkit for adding programmable guardrails to LLM-based conversational apps.  
You define rails in Colang — input, dialog, retrieval, and output checks — and the library enforces them around every model call so replies stay safe and on-topic.

```mermaid
flowchart LR
  user[user] --> rails[NeMo Guardrails]
  rails --> policy[Colang rails]
  rails --> llm[LLM]
  llm --> safe[on-topic reply]
```

The **Code samples** tab shows loading a config and generating a guarded reply.

## When to use it

Choose NeMo Guardrails when you need programmable, testable controls over an LLM
conversation — blocking off-topic or unsafe turns and steering dialog flows —
rather than relying on prompt instructions alone.
