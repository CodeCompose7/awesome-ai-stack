---
name: OpenHands
logo: /logos/openhands.svg
category: coding-autonomous
description: Open-source platform for autonomous AI software developers that edit code, run commands, and browse the web.
website: https://www.all-hands.dev
repo: https://github.com/All-Hands-AI/OpenHands
docs: https://docs.all-hands.dev
tags: [autonomous, software-engineering, sandbox, python]
language: Python
license: MIT
pricing: [open-source, free-tier, paid]
projects:
  - folder: openhands_1
    related: [litellm]
featured: true
samples:
  - version: "Docker · GUI"
    lang: bash
    diagram: |
      flowchart LR
        ui[localhost:3000] --> agent[OpenHands]
        agent --> sandbox[(sandboxed runtime)]
        sandbox --> diff[diff]
    code: |
      docker run -it --rm --pull=always \
        -e SANDBOX_RUNTIME_CONTAINER_IMAGE=docker.all-hands.dev/all-hands-ai/runtime:latest \
        -e LLM_MODEL="anthropic/claude-opus-4-8" \
        -e LLM_API_KEY="$ANTHROPIC_API_KEY" \
        -v /var/run/docker.sock:/var/run/docker.sock \
        -p 3000:3000 \
        docker.all-hands.dev/all-hands-ai/openhands:latest
    description: |
      ### When to use
      Reach for the Docker image for the quickest start, with a UI to supervise
      the agent.

      ### How it flows
      Open `http://localhost:3000` and describe the task; the agent edits code and
      runs commands in a sandboxed runtime.
    note: LLM_MODEL is a LiteLLM-style name, so you can point it at any provider.
  - version: "CLI · headless"
    lang: bash
    diagram: |
      flowchart LR
        task[task] --> cli[openhands CLI]
        cli --> sandbox[(sandbox)]
        sandbox --> diff[diff]
    code: |
      pip install openhands-ai

      export LLM_MODEL="anthropic/claude-opus-4-8"
      export LLM_API_KEY="$ANTHROPIC_API_KEY"

      # Headless/CLI mode — describe the task when prompted.
      openhands
    description: |
      ### When to use
      Reach for the CLI when you want to run the agent from a script or terminal,
      without the UI.

      ### How it flows
      `openhands` runs the same agent loop headless and shows the diff when it's
      done.
    note: For a runnable build using the SDK's local workspace (no nested Docker), see openhands_1 in the Implementation tab.
---

## Overview

OpenHands (formerly OpenDevin) runs an AI agent that works like a developer: it writes and edits code, executes shell commands in a sandboxed runtime, browses the web, and iterates until a task is done.  
You give it a goal and a repo; it does the multi-step work and shows you the diff.

```mermaid
flowchart LR
  goal[goal + repo] --> agent[OpenHands]
  agent --> sandbox[(sandboxed runtime)]
  sandbox -->|edit / run / browse| agent
  agent --> diff[diff]
```

The **Code samples** tab shows the Docker GUI and the headless CLI — pick from
the selector to compare.

## When to use it

Choose OpenHands when you want a full autonomous coding loop with a real
sandboxed environment (not just file edits) and a UI to supervise the agent.
