---
name: CrewAI
logo: /logos/crewai.png
category: frameworks
description: 역할 기반 멀티에이전트 오케스트레이션 — 목표와 작업을 가진 에이전트들을 crew로 묶습니다.
website: https://www.crewai.com
repo: https://github.com/crewAIInc/crewAI
docs: https://docs.crewai.com
tags: [multi-agent, orchestration, crew, python]
language: Python
license: MIT
pricing: [open-source, paid]
related: [langgraph, litellm]
samples:
  - version: "Python · one agent"
    lang: python
    diagram: |
      flowchart LR
        agents[agents] --> crew[Crew]
        tasks[tasks] --> crew
        crew --> out[result]
    code: |
      from crewai import Agent, Task, Crew

      # llm is a LiteLLM-style string, so any provider works by changing it.
      researcher = Agent(
          role="Researcher",
          goal="Explain tools clearly and briefly",
          backstory="A concise technical writer.",
          llm="anthropic/claude-opus-4-8",
      )

      task = Task(
          description="Summarize what CrewAI does in one sentence.",
          expected_output="a single sentence",
          agent=researcher,
      )

      crew = Crew(agents=[researcher], tasks=[task])
      print(crew.kickoff())
    description: |
      ### 언제 쓰나
      역할이 나뉜 에이전트들이 목표를 향해 작업을 협업하게 하고 싶을 때 CrewAI를
      쓰세요.

      ### 흐름
      에이전트(역할·목표)와 작업을 선언해 Crew로 묶고, `kickoff`이 작업을 실행합니다.
  - version: "Python · multi-agent"
    lang: python
    diagram: |
      flowchart LR
        r[Researcher] --> w[Writer]
        w --> out[tweet]
    code: |
      from crewai import Agent, Task, Crew, Process

      researcher = Agent(role="Researcher", goal="Find key facts",
                         backstory="Thorough.", llm="openai/gpt-4o")
      writer = Agent(role="Writer", goal="Write a tweet",
                     backstory="Punchy.", llm="openai/gpt-4o")

      facts = Task(description="List 3 facts about vector databases.",
                   expected_output="3 bullets", agent=researcher)
      tweet = Task(description="Turn the facts into one tweet.",
                   expected_output="a tweet", agent=writer)

      crew = Crew(agents=[researcher, writer], tasks=[facts, tweet],
                  process=Process.sequential)
      print(crew.kickoff())
    description: |
      ### 언제 쓰나
      한 에이전트가 다음으로 일을 넘기는 구조(조사 후 작성)일 때 여러 에이전트를
      순차로 쓰세요.

      ### 흐름
      순차 프로세스가 작업을 순서대로 실행하며 한 에이전트의 맥락을 다음으로
      넘깁니다.
---

## 개요

CrewAI는 역할이 다른 에이전트들을 **crew**(팀)로 묶어 목표를 향해 협업하게 합니다.  
각 에이전트는 역할·목표·배경을 갖고, crew에 작업 목록을 주면 `kickoff`이 순차 또는 병렬로 실행합니다.

```mermaid
flowchart LR
  agents[agents] --> crew[Crew]
  tasks[tasks] --> crew
  crew --> out[result]
```

**코드 샘플** 탭에는 단일 에이전트와 멀티에이전트 순차 실행 예시가 있습니다 —
선택기에서 골라 비교해 보세요.

## 언제 쓰면 좋은가

작업이 역할별로 자연스럽게 나뉘고(조사·작성·검토) 그 협업을 고수준으로 배선하고
싶을 때 CrewAI를 쓰세요.
