---
name: mini-SWE-agent
vendor: "Princeton NLP"
logo: /logos/mini-swe-agent.svg
category: coding-cli
updated: 2026-06-28
description: SWE-agent의 미니멀 후속작 — 약 100줄 코어로 GitHub 이슈를 자율 해결하며, 성능은 동등하지만 훨씬 단순합니다.
website: https://mini-swe-agent.com
repo: https://github.com/SWE-agent/mini-swe-agent
docs: https://mini-swe-agent.com/latest/
tags: [github-issues, swe-bench, autonomous, python]
language: Python
license: MIT
pricing: [open-source]
pricingNote: "오픈소스(MIT)라 무료. 자신의 LLM 키를 쓰며 사용하는 모델 비용만 부담합니다."
pricingSource: https://github.com/SWE-agent/mini-swe-agent
pricingCheckedAt: "2026-06-27"
related: [swe-agent]
projects:
  - folder: mini-swe-agent_1
    related: [litellm]
featured: true
samples:
  - version: "CLI · mini"
    lang: bash
    diagram: |
      flowchart LR
        task[task] --> agent[mini]
        agent -->|bash command| shell[(your shell)]
        shell -->|output| agent
        agent --> done([submit])
    code: |
      pip install mini-swe-agent

      # Interactive, in the current repo:
      mini

      # Or a one-off task with an explicit model:
      mini -m claude-opus-4-8 -t "Fix the failing test in tests/test_parser.py"
    description: |
      ### 언제 쓰나
      터미널에서 바로 에이전트를 돌리고 싶을 때 CLI를 쓰세요.

      ### 흐름
      에이전트가 bash 명령을 제안해 사용자의 셸에서 실행하고, 출력을 관찰하며
      작업이 끝날 때까지 반복합니다.
  - version: "Python · DefaultAgent"
    lang: python
    diagram: |
      flowchart LR
        model[LitellmTextbasedModel] --> agent[DefaultAgent]
        env[LocalEnvironment] --> agent
        agent -->|run task| loop[bash loop]
    code: |
      import yaml
      from minisweagent import package_dir
      from minisweagent.agents.default import DefaultAgent
      from minisweagent.environments.local import LocalEnvironment
      from minisweagent.models.litellm_textbased_model import LitellmTextbasedModel

      config = yaml.safe_load((package_dir / "config" / "default.yaml").read_text())["agent"]

      # The default config asks for a ```mswea_bash_command fence, so pair it
      # with the text-based model. MODEL is routed through LiteLLM.
      agent = DefaultAgent(
          LitellmTextbasedModel(model_name="anthropic/claude-opus-4-8"),
          LocalEnvironment(),
          **config,
      )
      result = agent.run("Print the result of 2 + 2, then submit.")
      print(result["exit_status"], result.get("submission", ""))
    description: |
      ### 언제 쓰나
      에이전트를 자신의 프로그램에 임베드하고 싶을 때 파이썬 바인딩을 쓰세요.

      ### 흐름
      DefaultAgent가 모델·환경·설정을 묶어 루프를 돌리고, 종료 시 exit_status와
      submission을 돌려줍니다.
    note: 실제로 돌려보려면 구현 샘플 탭의 mini-swe-agent_1을 보세요 — 성능 좋은 모델을 쓰세요.
---

## 개요

mini-SWE-agent는 [SWE-agent](../swe-agent/)의 간결한 후속작입니다.  
모델이 코드를 읽고 편집하고 실행해 GitHub 이슈를 고친다는 핵심 아이디어는 그대로지만, 에이전트 루프를 약 **100줄**로 줄였습니다 — 특수 도구 없이 셸 명령만 사용합니다.  
읽고, 고치고, 배포하기가 더 쉬우면서 SWE-bench에서도 경쟁력을 유지합니다.

```mermaid
flowchart LR
  task[task] --> agent[mini-SWE-agent]
  agent -->|bash command| shell[(shell)]
  shell -->|output| agent
  agent --> done([submit])
```

**코드 샘플** 탭에는 CLI와 파이썬 바인딩 두 가지 사용법이 있습니다 —
선택기에서 비교해 보세요.

## 언제 쓰면 좋은가

새 작업에는 SWE-agent보다 mini-SWE-agent를 선택하세요 — 활발히 개발되는
쪽이고, 작은 코어 덕분에 이해하고 커스터마이즈하기 쉽습니다.
