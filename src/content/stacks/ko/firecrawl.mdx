---
name: Firecrawl
category: tool-web
description: 어떤 URL이나 사이트든 크롤링·스크래핑해 LLM에 바로 쓰는 깔끔한 마크다운이나 구조화 데이터로 돌려줍니다.
website: https://www.firecrawl.dev
repo: https://github.com/firecrawl/firecrawl
docs: https://docs.firecrawl.dev
tags: [scraping, crawling, markdown, web]
language: TypeScript
license: AGPL-3.0
pricing: [open-source, free-tier, paid]
pricingNote: "AGPL-3.0 코어는 무료로 셀프 호스팅할 수 있고, Firecrawl Cloud는 크레딧 기반 호스팅 API입니다."
pricingTiers:
  - { plan: "Free", price: "$0", note: "체험용 월 1,000 크레딧." }
  - { plan: "Hobby", price: "$16/mo", note: "5,000 크레딧, 연 단위 결제." }
  - { plan: "Standard", price: "$83/mo", note: "100,000 크레딧, 더 높은 동시성." }
  - { plan: "Growth", price: "$333/mo", note: "500,000 크레딧, 우선 지원." }
  - { plan: "Enterprise", price: "Custom", note: "SSO, SLA, 데이터 무보존." }
pricingSource: https://www.firecrawl.dev/pricing
pricingCheckedAt: "2026-06-27"
related: [tavily, exa, llamaindex]
samples:
  - version: "Python · scrape"
    lang: python
    diagram: |
      flowchart LR
        url[URL] --> fc[Firecrawl]
        fc --> md[clean markdown]
        md --> llm[LLM]
    code: |
      from firecrawl import Firecrawl

      app = Firecrawl(api_key="fc-YOUR_API_KEY")

      # 한 페이지를 가져와 깔끔한 마크다운으로 돌려받습니다.
      doc = app.scrape("https://firecrawl.dev", formats=["markdown"])

      print(doc.markdown)  # LLM에 그대로 넣을 수 있습니다
    description: |
      ### 언제 쓰나
      에이전트가 잡음 많은 원본 HTML 대신 깔끔한 텍스트 형태의 웹 페이지가
      필요할 때 Firecrawl을 씁니다.

      ### 흐름
      `scrape`가 URL을 가져와 군더더기를 걷어내고 LLM에 바로 쓸 마크다운으로
      돌려줍니다.
---

## 개요

Firecrawl은 어떤 URL이나 사이트 전체든 LLM에 바로 쓰는 깔끔한 마크다운이나 구조화 데이터로 바꿔 줍니다.  
페이지 가져오기, 렌더링, 군더더기 제거까지 처리하므로 에이전트는 원본 HTML 대신 읽기 좋은 텍스트를 받습니다.

```mermaid
flowchart LR
  url[URL] --> fc[Firecrawl]
  fc --> md[clean markdown]
  md --> llm[LLM]
```

**코드 샘플** 탭에는 한 페이지를 마크다운으로 스크래핑하는 예시가 있습니다.

## 언제 쓰면 좋은가

검색이나 프롬프트에 쓸 웹 콘텐츠를 깔끔한 마크다운으로 받고 싶고, 스크래핑과
파싱 코드를 직접 짜고 관리하기는 번거로울 때 Firecrawl을 선택하세요.
