---
name: Cline
vendor: "Cline"
logo: /logos/cline.png
category: coding-ide
docVersion: "1.0"
updated: 2026-06-28
description: VS Code 안에서 계획·파일 편집·명령 실행을 수행하고 MCP 도구를 지원하는 자율 코딩 에이전트
website: https://cline.bot
repo: https://github.com/cline/cline
docs: https://docs.cline.bot
tags: [vscode, ide, mcp, plan-act]
language: TypeScript
license: Apache-2.0
pricing: [open-source]
pricingNote: "오픈소스(Apache-2.0)라 무료. 자신의 LLM 키를 쓰며 사용하는 모델 비용만 부담합니다."
pricingSource: https://github.com/cline/cline
pricingCheckedAt: "2026-06-27"
featured: true
samples:
  - version: "Install · VS Code"
    lang: bash
    diagram: |
      flowchart LR
        task[task] --> plan[Cline: plan]
        plan -->|approve each step| act[edit files / run commands]
        act --> done[done]
    code: |
      # Install the extension from the CLI (or search "Cline" in the Extensions panel)
      code --install-extension saoudrizwan.claude-dev
    description: |
      ### 언제 쓰나
      에디터 안에서 휴먼 인 더 루프 승인으로 에이전트를 돌리고 싶을 때 쓰세요.

      ### 흐름
      Cline이 계획을 제안하고, 각 단계 승인을 받아 파일을 편집하고 명령을
      실행합니다.
    note: 설치 후 Cline 패널에서 제공자(Anthropic 등)와 모델을 고르고 API 키를 붙여넣으세요.
  - version: "MCP · server config"
    lang: json
    diagram: |
      flowchart LR
        cline[Cline] -->|MCP| server[MCP server]
        server --> tools[external tools]
    code: |
      {
        "mcpServers": {
          "filesystem": {
            "command": "npx",
            "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/dir"]
          }
        }
      }
    description: |
      ### 언제 쓰나
      Cline에 파일시스템·검색 같은 외부 도구를 붙이고 싶을 때 MCP 서버를
      등록하세요.

      ### 흐름
      설정의 `mcpServers`에 명령을 추가하면 Cline이 그 서버가 노출하는 도구를
      호출할 수 있습니다.
---

## 개요

Cline(이전 이름 Claude Dev)은 에디터를 에이전트형 작업 공간으로 바꾸는 VS Code 확장입니다.  
먼저 계획을 제안하고, 각 단계마다 사용자의 승인을 받아 파일을 생성·편집하고 터미널 명령을 실행하며, Model Context Protocol(MCP)을 통해 외부 도구를 사용할 수 있습니다.  
API 키는 직접 준비합니다.

```mermaid
flowchart LR
  task[task] --> plan[Cline: plan]
  plan -->|approve each step| act[edit files / run commands]
  act -->|MCP| tools[external tools]
  act --> done[done]
```

**코드 샘플** 탭에는 설치와 MCP 서버 설정 예시가 있습니다 — 선택기에서 비교해 보세요.

## 언제 쓰면 좋은가

별도 앱이나 CI형 러너가 아니라, 휴먼 인 더 루프 승인과 MCP 도구 접근을 갖춘 자율 에이전트를 **에디터 안에서** 쓰고 싶을 때 Cline을 선택하세요.
