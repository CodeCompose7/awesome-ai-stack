---
name: Ollama
vendor: "Ollama"
logo: /logos/ollama.png
category: llm-local
updated: 2026-06-28
description: 명령 한 줄로 오픈 LLM을 로컬에서 실행 — 오프라인·프라이빗 에이전트를 위한 OpenAI 호환 API.
website: https://ollama.com
repo: https://github.com/ollama/ollama
docs: https://docs.ollama.com
tags: [local, open-models, self-host, runtime]
language: Go
license: MIT
pricing: [open-source, free-tier, paid]
pricingNote: "오픈소스라 로컬에서 모델을 무료로 실행합니다. Ollama Cloud는 Free·Pro $20/월·Max $100/월."
pricingSource: https://ollama.com/pricing
pricingCheckedAt: "2026-06-27"
related: [litellm]
projects:
  - folder: ollama_1
    related: [litellm]
samples:
  - version: "Shell · pull & run"
    lang: bash
    diagram: |
      flowchart LR
        cli[ollama run] --> srv[Ollama server]
        srv --> model[(local model)]
    code: |
      # Pull a model and chat locally — no API key, runs offline.
      ollama pull qwen3.5:9b
      ollama run qwen3.5:9b "Summarize Ollama in one line."
    description: |
      ### 언제 쓰나
      모델을 대화형으로 써보거나 실행 전에 받아둘 때 CLI를 쓰세요.

      ### 흐름
      `ollama`가 모델을 한 번 내려받은 뒤 로컬 11434 포트로 서빙합니다.
  - version: "Python · via LiteLLM"
    lang: python
    diagram: |
      flowchart LR
        app[your app] --> lite[LiteLLM]
        lite -->|ollama_chat/| srv[Ollama server]
    code: |
      import os
      from litellm import completion

      # ollama_chat/ uses Ollama's /api/chat endpoint, which supports tool calling.
      resp = completion(
          model="ollama_chat/qwen3.5:9b",
          messages=[{"role": "user", "content": "Summarize Ollama in one line."}],
          api_base=os.environ.get("OLLAMA_API_BASE", "http://localhost:11434"),
      )
      print(resp.choices[0].message.content)
    description: |
      ### 언제 쓰나
      같은 에이전트 코드를 로컬 모델과 클라우드 모델에서 모두 돌리려면 Ollama를
      LiteLLM으로 라우팅하세요.

      ### 흐름
      `ollama_chat/<model>`이 로컬 서버를 호출합니다. 도구 호출에는 generate가 아닌
      chat 엔드포인트(`ollama_chat/`)를 쓰세요.
    note: DooD를 쓰는 devcontainer에서는 OLLAMA_API_BASE=http://host.docker.internal:11434로 호스트 Ollama에 접근합니다.
---

## 개요

Ollama는 오픈 가중치 모델(Llama·Qwen·Gemma 등)을 명령 한 줄로 내 컴퓨터에서 실행하고,
`localhost:11434`에 **OpenAI 호환 API**를 띄웁니다.  
에이전트를 위한 로컬·무키 선택지입니다 — 무료·프라이빗·오프라인이며, 모델 문자열만
바꾸면 LiteLLM 기반 앱에 그대로 들어갑니다.

```mermaid
flowchart LR
  app[your app] --> srv[Ollama server]
  srv --> model[(local model)]
  model --> reply[reply]
```

**코드 샘플** 탭에는 CLI와 LiteLLM 경유 예시가 있습니다 — 선택기에서 골라 비교해
보세요.

## 언제 쓰면 좋은가

오프라인으로 개발하거나, 데이터를 기기 안에 두거나, API 비용을 피하고 싶을 때 Ollama를
쓰세요. 에이전트가 도구 호출이 필요하면 LiteLLM에서 `ollama_chat/` prefix를 쓰세요.
