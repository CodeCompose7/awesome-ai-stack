---
name: OpenHands
logo: /logos/openhands.svg
category: coding-autonomous
description: 코드를 편집하고 명령을 실행하며 웹을 탐색하는 자율 AI 개발자를 위한 오픈소스 플랫폼.
website: https://www.all-hands.dev
repo: https://github.com/All-Hands-AI/OpenHands
docs: https://docs.all-hands.dev
tags: [autonomous, software-engineering, sandbox, python]
language: Python
license: MIT
pricing: [open-source, free-tier, paid]
pricingNote: "오픈소스(MIT)라 무료로 셀프호스트할 수 있고, OpenHands Cloud는 에이전트를 대신 실행해 줍니다."
pricingTiers:
  - { plan: "Individual", price: "$0", note: "자체 API 키(BYOK)를 쓰거나, 제공자 원가 그대로 종량 과금." }
  - { plan: "Growth", price: "$500/월", note: "팀 시트, 상향된 한도." }
  - { plan: "Enterprise", price: "문의", note: "SSO·전담 지원·온프레미스." }
pricingSource: https://www.openhands.dev/pricing
pricingCheckedAt: "2026-06-27"
projects:
  - folder: openhands_1
    related: [litellm]
featured: true
samples:
  - version: "Docker · GUI"
    lang: bash
    diagram: |
      flowchart LR
        ui[localhost:3000] --> agent[OpenHands]
        agent --> sandbox[(sandboxed runtime)]
        sandbox --> diff[diff]
    code: |
      docker run -it --rm --pull=always \
        -e SANDBOX_RUNTIME_CONTAINER_IMAGE=docker.all-hands.dev/all-hands-ai/runtime:latest \
        -e LLM_MODEL="anthropic/claude-opus-4-8" \
        -e LLM_API_KEY="$ANTHROPIC_API_KEY" \
        -v /var/run/docker.sock:/var/run/docker.sock \
        -p 3000:3000 \
        docker.all-hands.dev/all-hands-ai/openhands:latest
    description: |
      ### 언제 쓰나
      에이전트를 감독할 UI와 함께 가장 빠르게 시작하고 싶을 때 Docker 이미지를
      쓰세요.

      ### 흐름
      `http://localhost:3000`을 열어 작업을 설명하면, 에이전트가 샌드박스 런타임에서
      코드를 편집하고 명령을 실행합니다.
    note: LLM_MODEL은 LiteLLM 형식이라 제공자를 자유롭게 바꿀 수 있습니다.
  - version: "CLI · headless"
    lang: bash
    diagram: |
      flowchart LR
        task[task] --> cli[openhands CLI]
        cli --> sandbox[(sandbox)]
        sandbox --> diff[diff]
    code: |
      pip install openhands-ai

      export LLM_MODEL="anthropic/claude-opus-4-8"
      export LLM_API_KEY="$ANTHROPIC_API_KEY"

      # Headless/CLI mode — describe the task when prompted.
      openhands
    description: |
      ### 언제 쓰나
      UI 없이 스크립트나 터미널에서 에이전트를 돌리고 싶을 때 CLI를 쓰세요.

      ### 흐름
      `openhands`가 같은 에이전트 루프를 헤드리스로 실행하고, 끝나면 변경 사항을
      보여줍니다.
    note: SDK의 로컬 워크스페이스를 쓰는(중첩 Docker 없는) 실행 예는 구현 샘플 탭의 openhands_1을 보세요.
---

## 개요

OpenHands(이전 이름 OpenDevin)는 개발자처럼 일하는 AI 에이전트를 실행합니다.  
샌드박스 런타임에서 코드를 작성·편집하고 셸 명령을 실행하며, 웹을 탐색하고, 작업이 끝날 때까지 반복합니다.  
목표와 저장소를 주면 다단계 작업을 수행한 뒤 변경 사항(diff)을 보여줍니다.

```mermaid
flowchart LR
  goal[goal + repo] --> agent[OpenHands]
  agent --> sandbox[(sandboxed runtime)]
  sandbox -->|edit / run / browse| agent
  agent --> diff[diff]
```

**코드 샘플** 탭에는 Docker GUI와 헤드리스 CLI 실행 예시가 있습니다 —
선택기에서 비교해 보세요.

## 언제 쓰면 좋은가

단순 파일 편집이 아니라 실제 샌드박스 환경을 갖춘 완전한 자율 코딩 루프와,
에이전트를 감독할 UI가 필요할 때 OpenHands를 선택하세요.
