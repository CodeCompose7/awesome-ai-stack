---
name: LangGraph
logo: /logos/langgraph.svg
category: frameworks
description: 분기·루프·휴먼 인 더 루프를 갖춘 상태 기반 다단계 에이전트를 위한 그래프 오케스트레이션.
website: https://www.langchain.com/langgraph
repo: https://github.com/langchain-ai/langgraph
docs: https://langchain-ai.github.io/langgraph/
tags: [orchestration, state-machine, multi-agent, python]
language: Python / JS
license: MIT
pricing: [open-source]
projects:
  - folder: langgraph_1
    related: [litellm, langchain]
featured: true
samples:
  - version: "0.2 · Graph API"
    lang: python
    diagram: |
      flowchart LR
        START([START]) --> think[think]
        think -->|steps < 3| think
        think -->|done| FIN([END])
    code: |
      from langgraph.graph import StateGraph, START, END
      from typing import TypedDict

      class State(TypedDict):
          input: str
          steps: int

      def think(state: State) -> State:
          return {"input": state["input"], "steps": state["steps"] + 1}

      def should_continue(state: State) -> str:
          return "think" if state["steps"] < 3 else END

      graph = StateGraph(State)
      graph.add_node("think", think)
      graph.add_edge(START, "think")
      graph.add_conditional_edges("think", should_continue)

      app = graph.compile()
      print(app.invoke({"input": "hello", "steps": 0}))
    description: |
      ### 언제 쓰나
      **그래프 API**는 명시적입니다. 노드와 그 사이의 엣지를 직접 선언합니다.
      분기·재시도, 또는 여러 단계에 걸친 체크포인트 상태가 필요할 때 선택하세요.

      ### 흐름
      `conditional edge`(조건부 엣지)가 정지 조건까지 `think`로 되돌아간 뒤
      `END`로 라우팅합니다.
  - version: "0.2 · Functional API"
    lang: python
    diagram: |
      flowchart LR
        IN([invoke]) --> task[task: think]
        task -->|loop x3| task
        task --> OUT([return])
    code: |
      from langgraph.func import entrypoint, task

      @task
      def think(value: int) -> int:
          return value + 1

      @entrypoint()
      def app(value: int):
          for _ in range(3):
              value = think(value).result()
          return value

      print(app.invoke(0))
    description: |
      ### 언제 쓰나
      **함수형 API**는 평범한 함수를 `@task`와 `@entrypoint`로 감쌉니다.
      명시적 그래프가 필요 없을 때 선택하세요.

      ### 흐름
      제어 흐름이 그냥 파이썬입니다 — `for` 루프가 `think`를 세 번 호출한 뒤
      반환합니다.
---

## 개요

LangGraph는 에이전트를 **노드들의 그래프**로 모델링하고 엣지로 연결합니다.
각 노드는 하나의 단계(모델 호출, 도구 실행, 메모리 갱신)이고, 엣지가 다음에
무엇을 실행할지 결정합니다. 상태가 명시적이기 때문에 평범한 while 루프로는
표현하기 어려운 지속적 실행, 루프, 분기, 휴먼 인 더 루프 일시정지를 얻습니다.

```mermaid
flowchart TD
  State[(State)] -.-> think
  subgraph Graph
    think[node: think] --> cond{conditional edge}
    cond -->|continue| think
    cond -->|end| done([END])
  end
```

**코드 샘플** 탭에는 같은 아이디어를 두 가지 방식으로 작성한 예시가 있습니다 —
선택기에서 API 버전을 골라 비교해 보세요.

## 언제 쓰면 좋은가

에이전트가 여러 단계에 걸친 순환, 재시도, 체크포인트된 상태가 필요할 때 —
즉 단일 선형 프롬프트 체인보다 더 구조적인 흐름이 필요할 때 LangGraph를
선택하세요.
