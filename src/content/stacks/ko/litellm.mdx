---
name: LiteLLM
logo: 🚅
category: llm-gateway
description: 100개 이상의 LLM API를 하나의 OpenAI 스타일 인터페이스로 호출하고, 라우팅·폴백·비용 추적 프록시까지 제공합니다.
website: https://litellm.ai
repo: https://github.com/BerriAI/litellm
docs: https://docs.litellm.ai
tags: [gateway, multi-provider, proxy, python]
language: Python
license: MIT
pricing: [open-source, paid]
pricingNote: "오픈소스 프록시는 무료로 셀프호스트하며 상위 모델과 인프라 비용만 부담합니다. LiteLLM Enterprise는 유료 지원·기능을 더합니다. 기본 약 $250/월부터 맞춤형."
pricingSource: https://docs.litellm.ai/docs/enterprise
pricingCheckedAt: "2026-06-27"
projects:
  - folder: litellm_1
    related: [anthropic-claude, langgraph]
featured: true
samples:
  - version: "SDK · completion()"
    lang: python
    diagram: |
      flowchart LR
        app[your code] -->|completion| litellm[LiteLLM]
        litellm --> prov[(any provider)]
    code: |
      from litellm import completion

      # One call shape for every provider — switch by editing only the model string.
      resp = completion(
          model="anthropic/claude-opus-4-8",
          messages=[{"role": "user", "content": "Summarize litellm in one line."}],
      )
      print(resp.choices[0].message.content)
    description: |
      ### 언제 쓰나
      프로바이더에 종속되지 않는 단일 호출만 필요할 때 SDK를 쓰세요. 모델
      문자열만 바꾸면 제공자가 바뀝니다.

      ### 흐름
      LiteLLM이 OpenAI 스타일 요청을 대상 제공자의 API로 변환하고, 응답을 다시
      정규화해 돌려줍니다.
    note: 이 호출을 실제로 돌려보려면 구현 샘플 탭의 litellm_1을 보세요.
  - version: "Router · fallbacks"
    lang: python
    diagram: |
      flowchart LR
        app[your code] --> router[Router]
        router -->|primary| A[claude-opus-4-8]
        router -.->|on error| B[gpt-4o]
    code: |
      from litellm import Router

      # Try the primary model; if it errors, fall back to the next one.
      router = Router(
          model_list=[
              {"model_name": "primary", "litellm_params": {"model": "anthropic/claude-opus-4-8"}},
              {"model_name": "backup",  "litellm_params": {"model": "openai/gpt-4o"}},
          ],
          fallbacks=[{"primary": ["backup"]}],
      )

      resp = router.completion(
          model="primary",
          messages=[{"role": "user", "content": "ping"}],
      )
      print(resp.choices[0].message.content)
    description: |
      ### 언제 쓰나
      가용성이 중요할 때 Router를 쓰세요. 기본 모델이 실패하면 자동으로 대체
      모델로 폴백하고, 여러 배포본에 걸쳐 로드 밸런싱도 합니다.

      ### 흐름
      `primary` 호출이 실패하면 Router가 `fallbacks` 매핑을 따라 `backup`으로
      재시도합니다.
---

## 개요

LiteLLM은 여러 모델 제공자(Anthropic, OpenAI, Bedrock, Vertex, 로컬 모델 등) 위에 얇은 추상화를 씌워 하나의 OpenAI 호환 호출로 노출합니다.  
SDK뿐 아니라 **프록시 서버**가 LLM 게이트웨이 역할을 합니다 — 로드 밸런싱, 자동 폴백, 재시도, 예산, 키별 비용 추적까지.  
덕분에 에이전트는 코드 변경 없이 모델을 교체할 수 있습니다.

```mermaid
flowchart LR
  agent[agent code] -->|OpenAI-style call| litellm[LiteLLM]
  litellm --> claude[Anthropic]
  litellm --> openai[OpenAI]
  litellm --> gemini[Gemini]
```

**코드 샘플** 탭에는 LiteLLM을 쓰는 두 가지 방식이 있습니다 —
선택기에서 SDK 호출과 Router 폴백을 비교해 보세요.

## 언제 쓰면 좋은가

에이전트가 제공자에 종속되지 않아야 하거나, 모델 간 폴백이 필요하거나, 예산을
강제하고 지출을 추적할 중앙 게이트웨이가 필요할 때 LiteLLM을 사용하세요.
