---
name: Guardrails AI
logo: /logos/guardrails-ai.png
category: guardrails
description: Guardrails Hub의 조합형 검증기로 LLM 입출력을 검증하고 구조화합니다.
website: https://guardrailsai.com
repo: https://github.com/guardrails-ai/guardrails
docs: https://guardrailsai.com/docs
tags: [guardrails, validation, safety, python]
language: Python
license: Apache-2.0
pricing: [open-source, paid]
pricingNote: "프레임워크와 Guardrails Hub는 오픈소스(Apache-2.0)라 무료로 셀프호스트합니다. Guardrails Pro는 맞춤 가격의 매니지드 엔터프라이즈 서비스입니다."
pricingSource: https://guardrailsai.com/pro
pricingCheckedAt: "2026-06-27"
related: [nemo-guardrails, instructor]
projects:
  - folder: guardrails-ai_1
    related: [litellm, openai]
samples:
  - version: "Python · validate output"
    lang: python
    diagram: |
      flowchart LR
        llm[LLM output] --> guard[Guard]
        guard --> validators[validators]
        guard --> ok[valid / fixed]
    code: |
      from guardrails import Guard, OnFailAction
      from guardrails.hub import RegexMatch  # guardrails hub install hub://guardrails/regex_match

      guard = Guard().use(
          RegexMatch,
          regex=r"\(?\d{3}\)?-? *\d{3}-? *-?\d{4}",
          on_fail=OnFailAction.EXCEPTION,
      )

      outcome = guard.validate("123-456-7890")
      print(outcome.validation_passed)  # -> True
    description: |
      ### 언제 쓰나
      PII 차단, 유효한 JSON, 정해진 형식처럼 LLM 입출력이 규칙을 지켜야
      사용자나 후속 코드에 도달하도록 보장하고 싶을 때 적합합니다.

      ### 흐름
      Hub 검증기를 `Guard`에 조합하면 `validate`가 텍스트를 점검해
      통과시키거나 수정하거나 실패 시 예외를 던집니다.
---

## 개요

Guardrails AI는 Guardrails Hub에서 가져온 조합형 검증기로 LLM 입출력을 검증하고 구조화하는 Python 프레임워크입니다.  
PII·탈옥·형식·사실성 같은 검증기를 모아 `Guard`를 만들면, 모델의 텍스트를 가로채 통과·수정·거부합니다.

```mermaid
flowchart LR
  llm[LLM output] --> guard[Guard]
  guard --> validators[validators]
  guard --> ok[valid / fixed]
```

**코드 샘플** 탭에는 Hub 검증기로 Guard를 만들고 출력을 점검하는 예시가 있습니다.

## 언제 쓰면 좋은가

LLM 입출력이 PII 제거, 스키마 강제, 탈옥 차단 같은 구체적 규칙을 지켜야 하고,
검사를 직접 짜기보다 Hub의 기성 검증기를 골라 조합하고 싶다면 Guardrails AI가
좋은 선택입니다.
