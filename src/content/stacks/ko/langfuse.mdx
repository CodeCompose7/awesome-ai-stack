---
name: Langfuse
vendor: "Langfuse"
logo: /logos/langfuse.svg
category: observability-tracing
docVersion: "1.0"
updated: 2026-06-28
description: LLM·에이전트 애플리케이션을 위한 오픈소스 트레이싱·평가·분석 도구.
website: https://langfuse.com
repo: https://github.com/langfuse/langfuse
docs: https://langfuse.com/docs
tags: [tracing, eval, monitoring, analytics]
language: TypeScript
license: MIT
pricing: [open-source, free-tier, paid]
pricingNote: "오픈소스라 무료로 셀프호스트할 수 있고, Langfuse Cloud는 관리형 관측 서비스입니다."
pricingTiers:
  - { plan: "Hobby", price: "$0", note: "월 5만 유닛." }
  - { plan: "Core", price: "$29/월", note: "상향된 한도." }
  - { plan: "Pro", price: "$199/월", note: "팀 기능." }
  - { plan: "Enterprise", price: "$2,499/월", note: "SSO·SLA·지원." }
pricingSource: https://langfuse.com/pricing
pricingCheckedAt: "2026-06-27"
projects:
  - folder: langfuse_1
    related: [anthropic-claude]
samples:
  - version: "TypeScript · trace"
    lang: ts
    diagram: |
      flowchart LR
        run[agent run] --> trace[trace]
        trace --> gen[generation span]
        trace --> tool[tool span]
        trace --> ui[(Langfuse UI)]
    code: |
      import { Langfuse } from 'langfuse';

      const langfuse = new Langfuse(); // reads LANGFUSE_* env vars

      const trace = langfuse.trace({ name: 'support-agent', userId: 'u_123' });

      const gen = trace.generation({
        name: 'plan',
        model: 'claude-opus-4-8',
        input: 'How do I reset my password?',
      });

      // ... call the model ...
      gen.end({ output: 'Tell the user to use the reset link.' });

      await langfuse.flushAsync();
    description: |
      ### 언제 쓰나
      각 단계를 직접 스팬으로 만들어 세밀하게 트레이싱하고 싶을 때 SDK를 쓰세요.

      ### 흐름
      트레이스 아래에 모델·도구 스팬을 중첩하고, 종료 시 flush로 Langfuse에
      전송합니다.
  - version: "Python · @observe"
    lang: python
    diagram: |
      flowchart LR
        fn["@observe function"] --> span[auto span]
        span --> ui[(Langfuse UI)]
    code: |
      from langfuse import observe, get_client

      @observe()  # wraps the call in a trace/span automatically
      def plan(question: str) -> str:
          # ... call the model ...
          return "Tell the user to use the reset link."

      plan("How do I reset my password?")
      get_client().flush()  # send buffered events before exit
    description: |
      ### 언제 쓰나
      함수 단위로 손쉽게 자동 계측하고 싶을 때 데코레이터를 쓰세요.

      ### 흐름
      `@observe`가 함수 호출을 감싸 스팬을 자동 생성하고, 입력·출력을 Langfuse로
      보냅니다.
    note: self-host로 트레이스를 왕복 검증하는 실행 예는 구현 샘플 탭의 langfuse_1을 보세요.
---

## 개요

에이전트는 로그만으로는 파악하기 어려운 방식으로 실패합니다.  
Langfuse는 각 실행을 중첩된 스팬(모델 호출, 도구 호출, 검색)의 **트레이스**로 기록해, 모든 단계의 지연·비용·정확한 프롬프트/출력을 들여다볼 수 있게 합니다.  
그 위에 점수와 평가를 덧붙일 수 있습니다.

```mermaid
flowchart LR
  run[agent run] --> trace[trace]
  trace --> gen[generation span]
  trace --> tool[tool span]
  trace --> ui[(Langfuse UI)]
```

**코드 샘플** 탭에는 TypeScript SDK와 Python 데코레이터 예시가 있습니다 —
선택기에서 비교해 보세요.

## 언제 쓰면 좋은가

다단계 에이전트 실행을 디버깅하거나, 프롬프트 버전을 비교하거나, 프로덕션에서
비용과 품질을 추적해야 할 때 Langfuse를 일찍 도입하세요.
