---
name: LM Studio
vendor: "LM Studio"
logo: /logos/lm-studio.webp
category: llm-local
updated: 2026-06-28
description: 로컬 LLM을 찾고 실행·서빙하는 데스크톱 앱 — OpenAI 호환 서버 내장.
website: https://lmstudio.ai
docs: https://lmstudio.ai/docs
tags: [llm, local, desktop, gguf]
language: App
license: proprietary
pricing: [free-tier, paid]
pricingNote: "개인·상업용 모두 무료. SSO·게이팅·비공개 협업이 필요하면 유료 Teams·Enterprise 옵션."
pricingSource: https://lmstudio.ai
pricingCheckedAt: "2026-06-27"
related: [ollama, vllm, litellm]
samples:
  - version: "Shell · local server"
    lang: bash
    code: |
      # In the LM Studio app: download a model, then start the local server.
      # It exposes an OpenAI-compatible API at http://localhost:1234/v1
      curl http://localhost:1234/v1/chat/completions \
        -H "Content-Type: application/json" \
        -d '{"model": "local-model",
             "messages": [{"role": "user", "content": "Say hello."}]}'
    description: |
      ### 언제 쓰나
      로컬 모델을 둘러보고 실행하는 친절한 데스크톱 UI에, 코드에 바로 꽂을 수 있는
      OpenAI 호환 엔드포인트까지 원할 때.
---

## 개요

LM Studio는 GGUF·MLX 형식의 로컬 LLM을 찾고 내려받아 실행하는 데스크톱 앱(macOS·Windows·Linux)으로, 채팅 UI와 내장 서버를 갖췄습니다.  
서버가 OpenAI 호환이라 기존 코드나 LiteLLM이 클라우드 제공자 대신 `localhost`를 가리키면 됩니다. 개인·상업용 모두 무료입니다.

```mermaid
flowchart LR
  hub[model catalog] --> lms[LM Studio]
  lms --> chat[chat UI]
  lms --> server[(OpenAI-compatible server)]
  server --> app[your app]
```

**코드 샘플** 탭에서 로컬 서버 호출을 보여줍니다.

## 언제 쓰면 좋은가

Ollama 같은 CLI 런타임이나 vLLM 같은 GPU 서빙 엔진보다, GUI로 로컬 모델을
가장 쉽게 둘러보고 내려받아 서빙하고 싶을 때 LM Studio를 고르세요.
